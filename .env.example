# Copy to ".env" for local development and fill in the values from your
# Supabase project (Project Settings -> API). Without these the app runs
# in local-only mode with no login.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
